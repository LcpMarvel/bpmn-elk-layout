{
  "name": "bpmn-with-elk",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "dev": "bun run --filter bpmn-viewer dev",
    "lint": "bun run --filter '*' lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
